# templates
Templates for judt Apps

# To make a new deployment file/zip do this from the node folder
npm run build && rm -rf node_modules .logs ../node-template.tar.gz && tar -czvf ../node-template.tar.gz .